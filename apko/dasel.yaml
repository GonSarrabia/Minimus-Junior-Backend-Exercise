contents:
  repositories:
    - https://dl-cdn.alpinelinux.org/alpine/edge/main
    - "@local ./packages"

  packages:
    - dasel@local

environment:
  HOME: /tmp
  USER: dasel

entrypoint:
  command: /usr/bin/dasel

archs:
  - x86_64