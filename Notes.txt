I ran the full pipeline using Docker-based commands for each step: melange keygen to generate a signing key, melange build to create the APK, melange test to verify the package, apko build to assemble the container image, docker load to load it, and bash tests/test.sh to validate the result. All four image tests passed successfully on linux/amd64 — the binary is present and runnable, the JSON query via stdin works, the architecture was confirmed as x86_64, and the CVE-2026-33320 YAML bomb was correctly rejected.

If I had more time, I would: a. parameterize the image tag and the tar file name in test.sh so there would be no need to edit the script if these values change, b. investigate and research how to inject the version string at build time so that dasel version reports a clean v3.3.1 instead of development-v3.3.1+dirty